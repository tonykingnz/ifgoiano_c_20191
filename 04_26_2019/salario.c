#include <stdio.h>
#include <stdlib.h>

int main (){
	float salario, reajuste;
	
	printf ("Escreva o seu salario:");
	scanf("%f", &salario)
	
	reajuste = (((salario / 100) * (5)) + (salario))
	if (salario )
	
	return 0;

}

